# Raymond Kusnadi — Portfolio

A responsive static portfolio. Open index.html to preview. No build dependencies.

Publish: GitHub Settings → Pages → Deploy from a branch → main / (root).

Edit content in index.html and appearance in style.css. Content and portrait adapted from https://github.com/maximilianusrk/MaximilianusRaymond. The portrait currently uses that public site's image URL.
